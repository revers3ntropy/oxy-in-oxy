export class Parser {
    tokens: List<Token>,

    def parse (tokens: List<Token>) AstNode {
        return (new Parser { tokens }).gen_ast()
    }

    def gen_ast (self) AstNode {
		return (new AstNode)
	}
}